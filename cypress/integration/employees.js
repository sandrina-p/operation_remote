describe('Employees', () => {
  // NOTE: IRL we would mock this page content, but here it's static.
  it('shows a list with 3 employees', () => {
    // Assert the list title
    // assert "add employee" href
    // Assert the list count
  })

  it('contains the correct data about the first employee', () => {
    // Note: There's a 99.9%% change all the other elements are correct because it is a loop.
    // If there is any edge case, perhaps it would be safer to assert it with a unit test (Jest + Enzyme).
  })
})
