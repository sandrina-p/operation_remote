describe('Employee: Add', () => {
  it('opens the page with an empty form to add a new employee', () => {})

  it('shows an error, on form submit with invalid fields', () => {})

  it('creates a new employee, on form submit with valid fields', () => {})

  it('shows the new employee, on the employee page', () => {})
})
